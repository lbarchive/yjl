Configuration File
==================

The default locations of configuration are (in order):
 1) Current directory/clis_cfg.py
 2) Current directory/clis_cfg
 3) ~/.clis_cfg.py
 4) ~/clis_cfg.py

If clis is run with `-c <configuration>`, then clis will try to load it first.

Keys
====

 Enter  : Print a seperator
 q      : Quit
 Ctrl+C : Quit
