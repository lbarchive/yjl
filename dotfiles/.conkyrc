# conky configuration
# edited by darcon@gmail.com
# edited by livibetter <http://j.mp/livibetter> 2010-01-13T19:44:27+0800

# set to yes if you want Conky to be forked in the background
background no

# Use Xft?
use_xft yes

# Xft font when Xft is enabled
#xftfont Bitstream Vera Sans Mono:size=8
xftfont ProggySquareTT

# Text alpha when using Xft
xftalpha 0.8

# Print everything to console?
out_to_console no
out_to_stderr no
# mail spool
#mail_spool $MAIL

# Update interval in seconds
update_interval 1.0

# This is the number of times Conky will update before quitting.
# Set to zero to run forever.
total_run_times 0

# Create own window instead of using desktop (required in nautilus)
#wn_window no

# For xcompmgr
own_window no
own_window_transparent yes
own_window_type desktop

# Use double buffering (reduces flicker, may not work for everyone)
double_buffer yes

# Minimum size of text area
minimum_size 200 5

# Maximum width
maximum_width 200

# Draw shades?
draw_shades yes

# Draw outlines?
draw_outline no

# Draw borders around text
draw_borders no

# Stippled borders?
stippled_borders 8

# border margins
border_margin 0

# border width
border_width 0

# Default colors and also border colors
default_color a0a0a0
#default_color white
default_shade_color black
default_outline_color a0a0a0
#default_outline_color white

# Text alignment, other possible values are commented
#alignment top_left
alignment top_right
#alignment bottom_left
#alignment bottom_right

# Gap between borders of screen and text
# same thing as passing -x at command line
gap_x 1475
gap_y 5

# Subtract file system buffers from used memory?
no_buffers yes

# set to yes if you want all text to be in uppercase
uppercase no

# number of cpu samples to average
# set to 1 to disable averaging
cpu_avg_samples 2

# number of net samples to average
# set to 1 to disable averaging
net_avg_samples 2

# Force UTF8? note that UTF8 support required XFT
override_utf8_locale yes

# Add spaces to keep things from moving about?  This only affects certain objects.
use_spacer right

# ${color lightblue}Volume:$color ${exec amixer get Master | egrep -o "[0-9]+%" | head -1 | egrep -o "[0-9]*"} % $alignr${color blue}${exec amixer get Master | grep -o "\[on\]" | head -1}$color${color red}${exec amixer get Master | grep -o "\[off\]" | head -1}$color
# ${image ~/.mpd-cover.jpg -p 0,820, -s 200x200}

#lua_load ~/.conky/weather_icons.lua
#${lua_parse conky_weather_icon TWXX0021}
# stuff after 'TEXT' will be formatted on screen
#PCM ${mixerbar pcm}
TEXT
${font Brushstroke:size=16}${color #c0c080}${time %A}${color #80c080}$alignr${time %Y}$color$font
${font Brushstroke:size=20}$alignc${color #8080c0}${time %B %d}$color$font
${color #c08040}${font Florence:size=28:weight=bold}$alignc${time %H:%M:%S}$font$color

${font Liberation Mono:size=10}${execpi 3600 DJS=`date +%_d`; cal -m | sed '1d' | sed '/./!d' | sed 's/$/                     /' | fold -w 21 | sed -n '/^.\{21\}/p' | sed 's/^/${alignc} /' | sed /" $DJS "/s/" $DJS "/" "'${color FCAF3E}'"$DJS"'${color #e0e0e0}'" "/}$font

${color #408040}${execi 1800 ~/.conky/weather.sh TWXX0021}

${color lightblue}Uptime:${color} $uptime
${color lightblue}Kernel:$color $kernel
${color lightblue}CPU:$color $cpu% ${acpitemp}°C$alignr$running_processes / $processes
${cpu cpu1} % ${freq 1} MHz$alignr${cpu cpu2} % ${freq 2} MHz
${color lightblue}Load: $color$loadavg

${color lightblue}Highest CPU:$color      PID   CPU%
${color #408040}${top name 1} ${top pid 1} ${top cpu 1}
${color}${top name 2} ${top pid 2} ${top cpu 2}
${color}${top name 3} ${top pid 3} ${top cpu 3}
${color}${top name 4} ${top pid 4} ${top cpu 4}

${color lightblue}Highest MEM:$color      PID   MEM%
${color #408040}${top_mem name 1} ${top_mem pid 1} ${top_mem mem 1}
${color}${top_mem name 2} ${top_mem pid 2} ${top_mem mem 2}
${color}${top_mem name 3} ${top_mem pid 3} ${top_mem mem 3}
${color}${top_mem name 4} ${top_mem pid 4} ${top_mem mem 4}

${color lightblue}MEM: $color$memperc% $mem$alignr$memmax
${membar 3,0}
${color lightblue}SWP: $color$swapperc% $swap$alignr$swapmax
${swapbar 3,0}

${color lightblue}DISK:$color Rd $diskio_read Wr $diskio_write
${diskiograph 10,0 000000 ffffff}
/   : ${fs_free /}/ ${fs_size /}
${fs_bar 3,0 /}
/tmp: ${fs_free /tmp}/ ${fs_size /tmp}

${color lightblue}eth0:$color
${addr eth0}$alignr${addr ppp0} 
Up: ${upspeed eth0} k/s$alignr${totalup eth0}
${upspeedgraph eth0 10,0 000000 ffffff}
Dn: ${downspeed eth0} k/s$alignr${totaldown eth0}
${downspeedgraph eth0 10,0 000000 ffffff}
${if_running wpa_supplicant}
${color lightblue}wlan0:$color ${wireless_essid wlan0} ${wireless_bitrate wlan0}
${wireless_link_qual_perc wlan0}% ${wireless_link_bar 3,0 wlan0}
${addr wlan0}$alignr${addr ppp1} 
Up: ${upspeed wlan0} k/s$alignr${totalup wlan0}
${upspeedgraph wlan0 10,0 000000 ffffff}
Dn: ${downspeed wlan0} k/s$alignr${totaldown wlan0}
${downspeedgraph wlan0 10,0 000000 ffffff}
${endif}
${color lightblue}Portage:$color ${execi 600 date -d "$(cat /usr/portage/metadata/timestamp.chk)" +%F\ %T}
VOL $if_mixer_mute${color #804040}$else${color #404080}$endif$mixerbar$color
BAT ${color #404080}$battery_bar$color
    ${if_match "$battery_short" != "F"}$battery_short($battery_time)$endif

${if_running mpd}
${color lightblue}MPD: ${color #408040}$mpd_status$color$alignr ${if_running mpdscribble}s$endif${if_match "$mpd_repeat" == "On"}r$endif${if_match "$mpd_random" == "On"}z$endif $mpd_vol%
${alignc}${color red}$mpd_title$color
${alignc}${color #9999ff}$mpd_album$color
${alignc}${color green}$mpd_artist$color
[$mpd_elapsed/$mpd_length] ${color #ccddff}${mpd_bar 3,0}$color
${endif}
${if_running shell-fm}${execp ~/bin/conky-shell-fm}$endif
